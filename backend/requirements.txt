fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
pytest>=8.0,<9.0
sqlalchemy>=2.0,<3.0
python-dotenv>=1.0,<2.0

# Postgres driver — only needed when DATABASE_URL points at Postgres, but
# harmless to always install so switching backends is just an env var away.
psycopg2-binary>=2.9,<3.0
